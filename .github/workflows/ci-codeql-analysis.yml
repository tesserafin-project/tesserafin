# Automatic triggers RESTORED on 2026-07-27 (#94). The July 2026 allocation
# refusal described in the parking stamp below is over: GitHub allocates
# hosted runners for this repository again. Evidence: run 30229812748, job
# "ABI - HEAD" completed/SUCCESS with 8 executed steps on runner "GitHub
# Actions 1000000000" (ubuntu-latest).
#
# The first CodeQL dispatch after that (run 30230125535) still failed, but for
# a DIFFERENT and repository-local reason: `autobuild` could not work out how
# to build this solution. It installed its own SDK ("dotnet_install: Error:
# Could not find `.NET Core SDK` with version = 10.0.0"), then tried building
# individual projects out of order and hit
# `error CS0234: The type or namespace name 'MimeTypes' does not exist in the
# namespace 'Tesserafin.Model'`, ending on "Could not auto-detect a suitable
# build method". `Perform CodeQL Analysis` was skipped as a result.
#
# The fix is the one GitHub's own error message prescribes: replace autobuild
# with the explicit build this repository actually uses. The query suite is
# UNCHANGED — `+security-extended` is retained.
#
# Still open: required status checks. GET .../branches/master/protection and
# GET .../rulesets both return 403 "Upgrade to GitHub Pro or make this
# repository public to enable this feature" (private repo, free organisation
# plan). This workflow reports a status automatically; nothing can make that
# status mandatory. #94 stays open for that reason.
#
# ── Parking stamp (historical, superseded by the restoration above) ────────
# (was) PARKED — manual trigger only (issue #62). Same allocation refusal as the
# other hosted workflows: red on every pull request and every push to master
# before the first step, plus a weekly cron that failed the same way and kept
# burning allocation attempts for nothing. Nothing is fixed here — the trigger
# is disarmed to stop the noise. See docs/local-ci.md.
#
# Loss of coverage is real and accepted for the duration of the outage: no
# static security analysis runs while this is parked. The local gate does NOT
# replace CodeQL. Restoring this is part of the return-to-normal checklist.
#
# Previous trigger: on: push (master) + pull_request (master) + schedule
# ('24 2 * * 4')
#
# ── Parking stamp ──────────────────────────────────────────────────────────
# Parked on : 2026-07-19
# Cause     : external, account-wide. GitHub refuses runner allocation before
#             the first step. all3f0r1 = free plan, all3f0r1/reefin = PRIVATE,
#             so Actions minutes are metered against a 2000 min/month
#             ACCOUNT-WIDE allowance. July 2026 weighted usage ~2646 min
#             (Linux 634 x1 + Windows 266 x2 + macOS 148 x10), drained mostly
#             by another repo. Spending limit is $0, so the overage is refused
#             rather than billed.
# Evidence  : run 29695198403 (ABI Compatibility) and run 29695198446
#             (OpenAPI Check), both `failure` in 3-4s with ZERO steps and no
#             annotations, while actions/permissions = {enabled: true,
#             allowed_actions: all} and every runs-on label is valid.
#             GET /actions/runners -> total_count: 0.
# Restore   : see the checklist in docs/local-ci.md (section 'Retour a la
#             normale'). The original trigger is recorded above verbatim.
# ───────────────────────────────────────────────────────────────────────────

name: "CodeQL"

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  schedule:
    - cron: '24 2 * * 4'
  workflow_dispatch:

permissions:
  contents: read
  security-events: write
  # Read-only; the CodeQL action needs it for its own run metadata. Without
  # it every step logs "Failed to gather information for telemetry: Resource
  # not accessible by integration".
  actions: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        language: [ 'csharp' ]

    steps:
    - name: Checkout repository
      uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

    - name: Setup .NET
      uses: actions/setup-dotnet@26b0ec14cb23fa6904739307f278c14f94c95bf1 # v5.4.0
      with:
        dotnet-version: '10.0.x'

    - name: Initialize CodeQL
      uses: github/codeql-action/init@8aad20d150bbac5944a9f9d289da16a4b0d87c1e # v4.36.2
      with:
        languages: ${{ matrix.language }}
        queries: +security-extended

    # NOT autobuild. Autobuild fails on this solution (run 30230125535): it
    # installs its own SDK, then builds projects out of order and dies on
    # CS0234 in Tesserafin.Model. Building the solution explicitly with the
    # SDK set up above is what GitHub's own error message prescribes, and it
    # is the same build the local gate runs.
    - name: Build solution for CodeQL
      run: dotnet build Tesserafin.sln --configuration Release -clp:ErrorsOnly

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@8aad20d150bbac5944a9f9d289da16a4b0d87c1e # v4.36.2
