<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="../Reefin.Playback.Decision/Reefin.Playback.Decision.csproj" />
    <ProjectReference Include="../../Reefin.Model/Reefin.Model.csproj" />
    <!-- PR114a: PlaybackExecutionPlanAdapter needs both PlaybackExecutionPlan (this reference) and
         the legacy Reefin.Model.Dlna.StreamInfo it adapts into (already referenced above) - this
         project already straddles both worlds for the request-side adapters
         (DlnaPlaybackAdapter/ReverseDlnaAdapter), so it is the natural home for the decision-side one
         too. No cycle: Reefin.Playback.Execution references only Reefin.Playback.Decision. -->
    <ProjectReference Include="../Reefin.Playback.Execution/Reefin.Playback.Execution.csproj" />
  </ItemGroup>

</Project>
