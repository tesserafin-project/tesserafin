<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Deliberately only Reefin.Playback.Decision: the builder (PlaybackExecutionPlanBuilder) is a
         pure projection of an already-computed PlaybackDecision, never the Reefin.Playback.Engine
         algorithm that produced it and never Reefin.Model/Reefin.Controller (PR114a task scope: this
         project must not see the legacy DLNA/StreamInfo world - that translation is the temporary
         adapter's job, in Reefin.Playback.Dlna, which already straddles both). -->
    <ProjectReference Include="../Reefin.Playback.Decision/Reefin.Playback.Decision.csproj" />
  </ItemGroup>

</Project>
